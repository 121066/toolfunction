## 工具函数

```
封装了开启和关闭的函数 fullscreenToggel()直接调用

判断浏览器是否是全屏 fullscreenEnable ()

开启全屏 reqFullScreen

关闭全屏 exitFullScreen

对象深拷贝 deepClone

根据value找index findArray(字典数组,value)

去除空格 trim

判断是否为空 isEmpty

解析url参数 getSearch

去除数组中无用值 compact

数组排序 tempSort

数组交换 tempArray([],index,index)

数组转千分位 numStr

滚动到指定区域 smoothScroll('#fooBar'); smoothScroll('.fooBar')

防抖 _debounce(fn,time)

节流 _throttle(fn,time)

获取本地缓存 getItem(key)

添加本地缓存 setItem(key,val)

删除指定缓存 removeItem(key)

删除全部缓存 removeAll()

校验手机 isPhone

校验银行卡 isBankCard

校验身份证 ispCardNo

```
